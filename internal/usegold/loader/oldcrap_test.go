package loader

import (
	"testing"
)

// only run locally, not on travis

func TestLoadTutorialFromGitHub(t *testing.T) {
	//dn, err := Load([]string{"git@github.com:monopole/mdrip.git"})
	//if err != nil {
	//	t.Fatal(err)
	//}
	//fmt.Printf("%s: %v\n", dn.Name(), dn)
}
